# Конспекти з математики

Сайт для читання конспектів і повторення флеш-картками. Формули, схеми, картки.

Зараз тут **16 конспектів** і **490 карток** — переважно геометрія трикутника.

## Що всередині

**Геометрія → Трикутники**

| № | Конспект | Картки |
|---|---|---|
| 1 | Види трикутників | 28 |
| 2 | Співвідношення між сторонами і кутами | 31 |
| 3 | Ознаки рівності трикутників | 27 |
| 4 | Ознаки подібності трикутників | 35 |
| 5 | Визначні лінії у трикутнику | 33 |
| 5.1 | Властивості медіан | 28 |
| 5.2 | Властивості бісектрис | 26 |
| 5.3 | Властивості висот | 17 |
| 5.4 | Властивості серединних перпендикулярів | 25 |
| 5.5 | Властивості середньої лінії | 22 |
| 6 | Вписане і описане кола | 24 |
| 7 | Площа трикутника | 33 |
| 8 | Рівнобедрений трикутник | 40 |
| 9 | Рівносторонній трикутник | 35 |
| 10 | Прямокутний трикутник | 82 |

Конспекти з дробовим номером (5.1–5.5) — **підтеми**: вони розбирають те, що
тема 5 лише називає. У списку на сайті вони з відступом.

**Алгебра**

| Конспект | Картки |
|---|---|
| Квадратні рівняння | 4 |

## Як це працює

Кожен конспект — **один текстовий файл** у теці `notes/`. Сайт сам знаходить
усі такі файли й будує з них сторінки, меню та колоди карток.

Файл поділений на три секції — це три режими на сайті:

- **Конспект** — теорія, яку читаєш;
- **Картки** — флеш-картки: натискаєш, картка перевертається;
- **Завдання** — задачі з прихованими розв'язками.

Будь-яку секцію можна пропустити — вкладка просто не з'явиться.

Формули набрані в LaTeX і рендеряться через [KaTeX](https://katex.org).
Схеми намальовані як SVG прямо в тексті конспекту: вони чіткі на будь-якому
екрані й самі підлаштовуються під світлу й темну тему.

## Запуск

Потрібен лише Python 3 — жодних залежностей ставити не треба.

```bash
cd math-notes
python3 serve.py
```

Далі відкрити http://localhost:8765. Зупинити — `Ctrl+C`.

`serve.py` перескладає список конспектів на кожен запит і забороняє браузеру
кешувати сайт, тому зміни видно одразу після оновлення сторінки.

**Але це стосується лише конспектів.** Якщо правиш `build.py`, `serve.py` або
щось у `src/` — сервер треба **перезапустити**: він тримає в пам'яті ту версію
коду, з якою стартував.

**Потрібен інтернет.** KaTeX і marked підключені з CDN просто в `index.html`,
тому без мережі формули не покажуться — замість них буде сирий LaTeX.
Лікується вкладенням бібліотек у теку (≈300 КБ).

## Як додати конспект

1. Створити файл `notes/<розділ>/<тема>/<назва>.md`
2. Оновити сторінку в браузері

Нова тека всередині `notes/` автоматично стає новим розділом або темою.

## Формат файлу

```markdown
---
title: Види трикутників       ← назва на сайті
section: Геометрія            ← розділ
topic: Трикутники             ← тема (необов'язково; збирає підтеми разом)
order: 1                      ← порядок вивчення; дробовий (5.1) = підтема
---

## Конспект

Текст для читання. Формули в доларах: $x^2 + 1$
Формула окремим рядком — у подвійних доларах:

$$D = b^2 - 4ac$$

## Картки

### Питання на лицьовому боці

Відповідь, яка з'являється після перевертання.

### Який це трикутник?

<svg viewBox="0 0 90 85">…</svg>

---

Малюнок вище — на **питанні**: усе до рядка `---` належить лицьовому боку.

## Завдання

### Умова задачі

Розв'язок, прихований до натискання кнопки.
```

Кожне `###` усередині секції — окрема картка або окреме завдання.

**Увага:** всередині SVG не має бути порожніх рядків — Markdown на них
вважає, що HTML скінчився, і показує решту як код.

## Як працюють картки

Одна картка на екран. Натискаєш на неї — перевертається. Стрілки `←` `→`
гортають колоду, **Перемішати** тасує порядок. (У колоді з усієї теми ця
кнопка робить інше — див. наступний розділ.)

Клавіші: пробіл — перевернути, `←` `→` — назад і вперед.

Сайт нічого не запам'ятовує: ні оцінок, ні прогресу. Щоразу колода
починається з чистого аркуша — це навмисно.

## Усі картки теми

На сторінці теми, над списком конспектів, є **колода з усієї теми**: вона
збирає картки з усіх її конспектів і дає **50 випадкових**.

- набір нових карток — **щоразу**, коли заходиш;
- **Перемішати** тягне нові 50 з усієї теми, а не переставляє ті самі;
- з якого конспекту картка — видно **тільки на звороті**: на питанні тема
  підказувала б відповідь.

Блок з'являється сам у будь-якій темі, де є картки. Окремих файлів для нього
не треба — картки беруться з тих самих конспектів.

## Перед публікацією

На статичному хостингу (GitHub Pages, Netlify) сервера немає — там просто
лежать файли. Тому список конспектів треба скласти заздалегідь:

```bash
python3 build.py
```

Інакше сайт в інтернеті показуватиме **старий** список конспектів.

У корені має лишатись порожній файл **`.nojekyll`** — без нього GitHub Pages
пропускає сайт через Jekyll, і той з'їдає теку з `.md`. Файл уже на місці,
просто не видаляй його.

## Файли

- `notes/` — конспекти (єдине, що треба чіпати)
- `serve.py` — локальний сервер
- `build.py` — складає `notes/index.json` зі списком конспектів
- `index.html`, `src/app.js`, `src/style.css` — сам сайт
- `Handoff.md` — рішення й пастки: те, чого з коду не видно
- `.nojekyll` — порожній, потрібен для GitHub Pages

## Авторство

Тексти, схеми й код зроблені разом із **Claude** — через
[Claude Code](https://claude.com/claude-code) у терміналі.


## Ліцензія

Конспекти написані власними словами на основі шкільних довідників.
Математичні факти нікому не належать; текст, схеми й код — вільні
для використання.
